# Only needed if the frontend and backend are deployed separately.
# Leave unset for local dev — vite.config.js already proxies /api to localhost:8787.
# VITE_API_URL=https://your-backend.onrender.com
